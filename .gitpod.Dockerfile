FROM ghcr.io/itsleeds/tds

# Minimal Gitpod Dockerfile: use the prebuilt image from GHCR.
# Add extra system packages or user-level installs below only if Gitpod
# requires them (e.g., debugger tooling, system libs, etc.).
